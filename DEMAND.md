# 五子棋 App 需求文档

> 版本：1.0（依据 Android `com.github.lany192.gomoku` v1.0 代码现状整理）
> 目标：本文档是完整的产品与技术需求说明，**仅凭本文档可在 Android 与 iOS 上从零还原本 App**，并保证：
> 1. 双端体验一致；
> 2. 新老版本之间、不同平台之间的局域网/蓝牙联机可互通（有线协议字节级冻结）；
> 3. AI 对局行为可复现。

## 目录

- [1. 产品概述](#1-产品概述)
- [2. 信息架构与页面导航](#2-信息架构与页面导航)
- [3. 通用 UI/UX 规范](#3-通用-uiux-规范)
- [4. 棋局规则引擎](#4-棋局规则引擎)
- [5. 棋盘视图](#5-棋盘视图)
- [6. 本地双人对局](#6-本地双人对局)
- [7. 人机对战](#7-人机对战)
  - [7.1 对局行为](#71-对局行为)
  - [7.2 25 种 AI 算法](#72-25-种-ai-算法)
  - [7.3 5 档难度与参数](#73-5-档难度与参数)
  - [7.4 AI 引擎公共骨架](#74-ai-引擎公共骨架)
  - [7.5 各家族实现规格](#75-各家族实现规格)
  - [7.6 学习类引擎与权重持久化](#76-学习类引擎与权重持久化)
  - [7.7 确定性与线程约束](#77-确定性与线程约束)
- [8. 每步限时倒计时](#8-每步限时倒计时)
- [9. 局域网联机](#9-局域网联机)
  - [9.1 有线协议（字节冻结）](#91-有线协议字节冻结)
  - [9.2 发现与信令](#92-发现与信令)
  - [9.3 建连与角色](#93-建连与角色)
  - [9.4 对局状态机](#94-对局状态机)
  - [9.5 聊天](#95-聊天)
- [10. 蓝牙联机](#10-蓝牙联机)
- [11. 数据与持久化](#11-数据与持久化)
- [12. 全文案总表](#12-全文案总表)
- [13. 平台差异与 iOS 移植要点](#13-平台差异与-ios-移植要点)
- [14. 非功能需求](#14-非功能需求)
- [15. 验收标准](#15-验收标准)
- [16. 现状缺口与处置结论](#16-现状缺口与处置结论)

---

## 1. 产品概述

| 项 | 内容 |
|---|---|
| 产品名 | 五子棋 |
| 副标题 | 原木棋盘 · 墨绿对弈 |
| 形态 | 纯本地单机 App，无账号、无后端服务器；联机为同一局域网/蓝牙范围内的点对点直连 |
| 平台 | Android（minSdk 24 / targetSdk 36）；iOS 由本文档推导实现 |
| 语言 | 简体中文（当前无多语言） |
| 核心玩法 | 15×15 无禁手自由五子棋，横/纵/斜任意方向连成五子及以上即胜 |
| 对局模式 | ① 本地双人同屏 ② 人机（25 种算法 × 5 档难度）③ 局域网 WiFi 联机 ④ 蓝牙联机 |

### 1.1 功能总览

- **首页**：四个模式入口 + 关于对话框。
- **本地双人**：同屏轮流落子、每步限时 3 分钟、悔棋（1 步）、新开、局内比分。
- **人机对战**：人执黑先手 / AI 执白；可在对局中随时切换 25 种 AI 算法与 5 档难度（立即生效、持久化）；悔棋（人+AI 共 2 步）；学习类算法跨对局积累并持久化权重；无限时。
- **局域网联机**：UDP 组播自动发现同一 WiFi 设备（周期重播 + 离线淘汰）、请求/同意/拒绝握手、TCP 直连对局；支持悔棋（协商）、求和（协商）、认输（宣告）、新开同步、每步 3 分钟限时、断线提示（心跳假死检测）、聊天（可收可发）。
- **蓝牙联机**：附近/已配对设备列表、"请求即连接"式握手（RFCOMM + 单字节裁决）、对局通道与语义完全复用局域网实现（无聊天；心跳假死检测同样生效）。

---

## 2. 信息架构与页面导航

### 2.1 页面清单

| # | 页面 | Android 类 | 布局 | 标题 | 进入方式 |
|---|---|---|---|---|---|
| 1 | 首页 | `MainActivity` | `activity_main` | 无 Toolbar，自绘大标题「五子棋」 | 启动入口 |
| 2 | 人机对战 | `RobotGameActivity` | `game_single` | 人机对战 | 首页「人机对战」卡片 |
| 3 | 对手设置 | `AiSelectActivity` | `ai_select` | 对手设置 | 人机对局页「对手设置」卡片 |
| 4 | 两人对战 | `PersonGameActivity` | `game_fight` | 两人对战 | 首页「两人对战」卡片 |
| 5 | 联机（局域网） | `ConnectionActivity` | `activity_connect` | 联机 | 首页「联网对战」卡片 |
| 6 | 联机（蓝牙） | `BluetoothConnectActivity` | `activity_connect`（复用） | 联机 | 首页「蓝牙对战」卡片 |
| 7 | 联网对局（局域网） | `NetGameActivity` | `game_net` | 联网对战 | 局域网握手成功后进入 |
| 8 | 联网对局（蓝牙） | `NetGameActivity`（复用） | `game_net` | 蓝牙对战 | 蓝牙握手成功后进入 |

### 2.2 导航图

```
首页
├─ 人机对战 ──> 人机对局页 ──「对手设置」──> 对手设置页（返回即应用）
├─ 两人对战 ──> 本地双人对局页
├─ 联网对战 ──> 局域网联机页 ──握手成功──> 联网对局页
└─ 蓝牙对战 ──> 蓝牙联机页 ──握手成功──> 联网对局页
```

- 所有二级页 Toolbar 左侧为返回箭头，点击 `finish()` 返回上一页（系统返回键同）。
- 联机页在进入对局页后**自身出栈**（`finish()`），返回键从对局页直接回首页。

### 2.3 页面间参数

| 传递 | 内容 |
|---|---|
| 联机页 → 对局页 | `isServer`（Bool：被请求方=true）、`peer`（对端地址：局域网为 IP、蓝牙为 MAC）、`bluetooth`（Bool：true 用蓝牙传输，对局页据此把标题设为「蓝牙对战」） |
| 对局页 → 对手设置页 → 对局页 | 携带当前 `algorithm`（枚举名）、`level`（序号）；返回 `RESULT_OK` + `EXTRA_ALGORITHM`（枚举名）、`EXTRA_LEVEL`（ordinal）；解析失败则保持原值 |

---

## 3. 通用 UI/UX 规范

### 3.1 主题与配色

Material 3 风格（DayNight 自动浅/深色），无 ActionBar（各页面自绘 Toolbar）。

**色彩令牌（浅色 / 深色）**

| 令牌 | 浅色 | 深色 | 用途 |
|---|---|---|---|
| colorPrimary | `#2F5D4C` 墨绿 | `#9FCBB4` | 主色（标题、图标着色、选中态） |
| colorSecondary | `#8A6B45` 暖棕 | `#D6B98C` | 辅色 |
| colorBackground | `#F7F4EC` 米色 | `#14120E` | 背景 |
| colorSurface | `#FDFAF3` | `#1B1914` | 表面 |
| colorError | `#BA1A1A` | `#F2B8B5` | 错误/危险/倒计时告警 |
| board_surface | `#E6C48C` | `#C99C63` | 棋盘底色 |
| board_frame | `#C89E60` | `#A67C45` | 棋盘外框（木框卡片） |
| board_line | `#6B4E2E` | `#3E2E1C` | 棋盘格线 |
| stone_black | `#3C3F41` | 同左 | 黑子填充 |
| stone_white | `#F5F4F0` | 同左 | 白子填充 |
| stone_white_stroke | `#4A4640` | 同左 | 白子描边 |
| win_mark | `#D54533` | `#FF6E5E` | 胜利连线高亮 |

**尺寸令牌**：棋盘线宽 1dp；星位半径 3dp；胜利线宽 2.5dp；间距 4/8/16/24dp（xs/s/m/l）。

### 3.2 布局规范

- 所有页面根布局为垂直 `ScrollView`（除首页为 FrameLayout），内容内边距 16dp。
- 全面屏：内容绘制到系统栏之下，把 system bars + 刘海 insets 叠加到根布局 padding 上（Android `enableEdgeToEdge()`；iOS 对应 safeAreaInsets）。
- 不锁定横竖屏；旋转/配置变更后由状态重放恢复（界面渲染必须幂等）。
- 二级页顶部：透明背景 Toolbar（返回箭头 + 标题）。

### 3.3 通用组件

| 组件 | 规范 |
|---|---|
| 模式入口卡片 | 圆角卡片 + 40dp 主色图标 + 两行文本（主标题 TitleMedium / 副标题 BodySmall 次要色）+ 右侧 24dp「>」箭头；整卡可点，带水波纹 |
| 记分卡 | 描边卡片（1dp outlineVariant，无阴影）：左黑方区 → 冒号「:」→ 右白方区（镜像）；每方 = 棋子圆点 24dp + 名称 + 8dp 当前行动指示点（绿点，非行动方隐藏）+ 胜场数 |
| 对局按钮 | 主按钮（filled）：新开；次按钮（outlined）：悔棋/求和；危险按钮（outlined，error 色文字与描边）：认输；等宽平铺一行 |
| 终局横幅 | 主色容器色卡片（圆角 12dp），文字 TitleMedium；**用横幅常驻展示，不用弹窗**；由状态幂等渲染 |
| 对话框 | 不可取消（裁决类：悔棋/求和/联机请求）或可取消（等待类/关于/认输确认）两种；按钮文案固定见 §12 |
| Toast | 一次性轻提示，见 §12 文案表 |

### 3.4 首页详述

- 大标题「五子棋」（DisplaySmall、主色、上边距 48dp）+ 副标题「原木棋盘 · 墨绿对弈」。
- 卡片区整体偏下（标题与卡片之间为弹性空白），四张卡片自上而下、间距 16dp：

| 卡片 | 标题 | 副标题 | 图标语义 | 动作 |
|---|---|---|---|---|
| 1 | 人机对战 | 25 种算法 × 5 档难度 | 机器人 | → 人机对战页 |
| 2 | 两人对战 | 同屏轮流落子 | 双人剪影 | → 本地双人页 |
| 3 | 联网对战 | 连接同一 WiFi 的玩家 | WiFi 扇形 | → 局域网联机页 |
| 4 | 蓝牙对战 | 连接附近的蓝牙设备玩家 | 蓝牙符号 | → 蓝牙联机页 |

- 右上角「关于」图标按钮 → 对话框：标题「关于」，正文「欢迎访问源代码\nhttps://github.com/lany192/FiveChess」，按钮「确定」。

---

## 4. 棋局规则引擎

> 实现要求：与 UI 解耦、无平台依赖（Android 侧为纯 Kotlin，可直接移植）；内部持可变棋盘，对外只暴露不可变快照与事件列表。

### 4.1 基本规则

| 项 | 规则 |
|---|---|
| 棋盘 | 15×15，共 225 个落子点 |
| 坐标系 | `x` = 横向列（0..14，向右增大），`y` = 纵向行（0..14，向下增大） |
| 棋子编码 | `0=空 / 1=黑 / 2=白`（AI 与网络层）；领域侧枚举 `BLACK / WHITE`，`toCode()` 1/2 |
| 先手 | 黑方先手（所有模式） |
| 禁手 | 无（自由五子棋，不判三三/四四/长连禁手） |
| 胜负 | 只看**刚落下的这一子**所在的 4 个方向（横、纵、正斜、反斜）；任一方向连续同色 **≥5 子即胜**（六连、七连同样判胜，不做"恰好五连"限制） |
| 胜利连线 | 返回完整连续同色段（按端点排序），用于 UI 高亮；六连高亮 6 个点 |
| 平局 | **满盘即和棋**：第 225 手落定后若未成五连，判和棋（`over=true`、`winner=null`）；**五连优先**（最后一手同时成五与满盘时按胜局处理，六连/七连仍判胜）。悔棋可解除和棋。**联机满盘和棋要求两端同版本**（旧版无此逻辑，第 225 手后会永久失步，性质与 TIMEOUT 相同） |

### 4.2 状态模型

引擎内部状态：`mode`、`mySide`（可选）、`first`（默认黑）、棋盘、落子历史（有序）、`active`（当前行棋方）、`winner`、`over`。

对外快照字段（不可变）：

| 字段 | 说明 |
|---|---|
| `mode` | `LOCAL_TWO` / `AI` / `LAN` / `BLUETOOTH` |
| `width` / `height` | 15 / 15 |
| `cells[x][y]` | 每格 `Side?`（null=空） |
| `moves` | 落子历史（按时间序） |
| `active` | 当前行棋方 |
| `winner` / `over` | 胜者 / 是否终局 |
| `drawn` | 是否满盘和棋（派生自 `over && winner == null`；悔棋/重开随 `over` 一同复位） |
| `mySide` | 本端执子；null 表示本地双人（双方均可操作） |
| 派生 | `lastMove: Point?`、`sideAt(x,y)`（越界返回 null）、`canMove(side)` |

### 4.3 操作与校验

| 操作 | 语义 |
|---|---|
| `start(mode, mySide?, first=BLACK)` | 重置一切（清盘、清历史、active=first、winner=null、over=false）；返回快照 + 空事件 |
| `applyMove(x,y)` | 本端落子：非法则返回 `IllegalMove` 事件且不改状态；`mySide==null`（双人）时落给当前 active 方，否则要求 `active == mySide` |
| `applyRemoteMove(x,y,side)` | 远端（AI/网络）落子：**指定落子方、不校验轮次**；仍校验越界/终局/占用 |
| `declareTimeout(loser)` | 超时判负：winner = loser 的对方，over=true，active 归胜方；**已终局时为 no-op**（不会被后到的宣告翻盘） |
| `rollback(steps=1)` | 从历史尾部回退 steps 手，逐子清空；**只要移除非空即清除 winner/over**（悔棋可解除五连/超时/和棋终局）；`active = 最后一手的对方`，历史为空则回 `first`（黑）；空历史安全 |
| `restart()` | 清盘重开，active=first（黑先） |
| `snapshot()` | 导出深拷贝快照（外部持有者修改不影响引擎） |

**落子校验优先级**：越界 → 终局 → 点位已占 → 非本方回合（`IllegalMove.Reason` = `OUT_OF_BOUNDS / GAME_OVER / OCCUPIED / NOT_YOUR_TURN`）。

**终局时的 active 语义**：五连胜与超时判负后 `active` 均停留在/归到**胜方**，满盘和棋后 `active` 停留在**最后一手方**；三种终局棋盘均锁定（再去落子返回 `GAME_OVER`）。

### 4.4 事件类型

| 事件 | 载荷 | 触发 |
|---|---|---|
| `MoveApplied` | `move` + `nextActive` | 普通落子 |
| `GameOver` | `winner` + `line`（连珠点列表） | 五连（优先于满盘判定） |
| `Draw` | 无 | 满盘未成五连（`over=true`、`winner=null`） |
| `Timeout` | `loser` + `winner`（无连珠线） | 超时判负 |
| `IllegalMove` | x、y、reason | 非法落子 |
| `RollbackApplied` | `removed`（被移除的 Move 列表）+ `nextActive` | 悔棋 |
| `Restarted` | 无 | 重开 |

---

## 5. 棋盘视图

> 棋盘为自绘视图（Android SurfaceView），**自身不含任何对局逻辑**：唯一渲染入口 `render(state)`、唯一输入回调 `onCellTapped(x,y)`。

### 5.1 渲染规格

| 项 | 规格 |
|---|---|
| 尺寸 | 正方形；边长向 15 取整整除，保证每格整数像素；`cellSize = 边长 / 15` |
| 格线 | 15 条竖线 + 15 条横线；首线偏移 `cellSize/2`；**落子点在线的交点**（共 15×15 交点） |
| 星位 | 5 个实心圆点（半径 3dp），位于 (7,7) 与 (3,3)/(11,3)/(3,11)/(11,11) |
| 棋子 | 直径=格宽，圆心落在交点上；黑子纯色圆 `#3C3F41`；白子 `#F5F4F0` + 描边 `#4A4640` |
| 最后一手标记 | 棋子中心加一个对比色小圆点（黑子上白点 / 白子上深色点） |
| 胜利高亮 | 连珠段首尾连一条直线（2.5dp，win_mark 色），并在**每个获胜点**画一圈半径 `cellSize/2 − 线宽` 的圆 |
| 外框 | 棋盘视图外包一层直角木框卡片（board_frame 色，内边距 10dp，有轻微阴影） |

### 5.2 交互规格

- 按下（ACTION_DOWN）：记录按下格并显示**焦点框**（48×48 viewport 的圆角方框，3dp 主色描边）。
- 抬手（ACTION_UP）：若抬手点仍在**按下点 ±2 格**范围内，则以按下格触发 `onCellTapped(x,y)`（允许轻微滑动的手感）；超出容差视为拖动，忽略。
- 非法落子（越界/占用/非本方回合/已终局）**无任何视觉或文字反馈**，静默忽略。
- 无缩放、无拖动平移、无坐标标注（A–O / 1–15）、无音效。

### 5.3 渲染模型

`BoardRenderState(width, height, cells[x][y], lastMove, winLine)`；由对局状态映射，全空初始（`empty(15,15)`）。渲染调用可来自任意线程，内部切主线程。

---

## 6. 本地双人对局

### 6.1 页面元素

Toolbar「两人对战」→ 记分卡（黑方 / 白方 + 胜场）→ 倒计时 TextView → 终局横幅（默认隐藏）→ 棋盘 → 按钮行「新开」「悔棋」。

**无**求和、无认输、无聊天。

### 6.2 行为需求

| 项 | 需求 |
|---|---|
| FR-PERSON-1 落子 | 点击空格落当前行棋方的子；黑先；非法点击无反馈且**不重置倒计时** |
| FR-PERSON-2 悔棋 | 回退 **1 手**（不论归属）；无子时安全无操作；悔棋后清除终局态、横幅消失、倒计时重置为满 |
| FR-PERSON-3 新开 | 清盘、黑先、横幅消失、倒计时重置；**比分保留** |
| FR-PERSON-4 计分 | 五连 → 胜方胜场 +1；超时 → 判负方的对方胜场 +1；**满盘和棋 → 双方均不加胜场**；比分仅会话内内存，退出页面即清零（不持久化） |
| FR-PERSON-5 计时 | 每步 3 分钟，倒计时见 §8 |
| FR-PERSON-6 结果文案 | 五连：`黑方胜！` / `白方胜！`；超时：`黑方超时判负，白方胜！` / `白方超时判负，黑方胜！`；满盘和棋：`双方和棋`（均为横幅常驻展示） |

---

## 7. 人机对战

### 7.1 对局行为

| 项 | 需求 |
|---|---|
| FR-AI-1 角色 | **本端（人）固定执黑先手，AI 固定执白**。引擎编码 0/1/2，AI 视角 self=白(2)、rival=黑(1) |
| FR-AI-2 页面元素 | Toolbar「人机对战」→ 记分卡（自己 / 电脑 + 胜场）→ **对手设置卡片**（文案 `算法：%1$s · 难度：%2$s`，右侧箭头，点击进入对手设置页）→ 终局横幅 → 棋盘 → 「新开」「悔棋」。**无倒计时**（此模式不限时、无超时判负） |
| FR-AI-3 思考 | 玩家落子后轮到 AI 时后台计算（单线程）；思考期间不锁 UI、无「思考中」提示 |
| FR-AI-4 悔棋 | 回退 **2 手**（AI 一子 + 人一子）；AI 思考中点悔棋 → 记 pending，等本次 AI 落子落地后**一次性**回退 2 手 |
| FR-AI-5 思考中落子 | 忽略（引擎按非本方回合拒绝；不并发搜索、不重复落子） |
| FR-AI-6 思考中重开 | 重开立即生效；过期返回的 AI 落子按轮次校验丢弃 |
| FR-AI-7 换算法 | 思考中更换 → 先持久化，本次搜索仍用旧引擎，落子落地后切换新引擎（任一时刻只有一个引擎在跑）；非思考中更换立即生效 |
| FR-AI-8 换难度 | 立即写入当前引擎实例 + 持久化；**思考中改档不影响本次搜索结果**（引擎在入口快照档位） |
| FR-AI-9 结果 | 五连：`黑方胜！`（人胜）/ `白方胜！`（AI 胜）；满盘和棋：`双方和棋`（双方不加胜场）；均为横幅；比分仅内存 |
| FR-AI-10 学习钩子 | 终局（含满盘和棋，AI 可能落第 225 手）/ 悔棋 / 重开时通知当前 AI 实例：和棋以 `GameOutcome(winner=null)` 告知（学习类据此更新/落库权重，见 §7.6） |
| FR-AI-11 对手设置页 | 见下 |

### 7.1.1 对手设置页（AiSelectActivity）

- 结构：Toolbar「对手设置」→ 小标题「难度」→ 5 个单选 Filter Chip（不换行限制，可换行）：`入门 / 简单 / 中等 / 困难 / 大师` → 小标题「算法」→ 分组列表。
- 列表按 6 个家族分组（家族头 + 算法行），家族头用主色小标题；算法行 = 名称 + 右侧主色勾选图标（选中显示）。
- **点击行即选中，无「确定」按钮；任何返回路径都即"返回即应用"**：
  - 页面本身不写持久化（避免"看着改了其实没应用"的双真相）；
  - 返回时带回 算法枚举名 + 难度序号，由对局页落库并生效。
- 默认值：难度「中等」，算法「棋型评分（经典）」。

### 7.2 25 种 AI 算法

> 枚举声明顺序 = UI 展示顺序；同家族必须连续排列。默认 = 棋型评分（经典）。
> 持久化按**枚举名**字符串（改名即回退默认）。新增算法需同步：引擎工厂、UI 文案映射、文案表（前两处要求编译期穷尽检查）。

| # | 枚举名 | 家族 | 界面名 | 原理概述 |
|---|---|---|---|---|
| 1 | `MINIMAX` | 搜索类 | 极小极大 | 纯极小极大（不剪枝、不早停），教学用 |
| 2 | `ALPHA_BETA` | 搜索类 | Alpha-Beta 剪枝 | 负极大 + αβ 剪枝 |
| 3 | `PVS` | 搜索类 | 主要变例搜索 | 首着全窗、其余零窗试探失败重搜 |
| 4 | `MTD_F` | 搜索类 | MTD(f) 零窗口迭代 | 零窗反复收敛，靠置换表根着法重启 |
| 5 | `ITERATIVE_DEEPENING` | 搜索类 | 迭代加深 | 逐层加深 + 渴望窗口；中断层整层丢弃 |
| 6 | `TRANSPOSITION_TABLE` | 搜索类 | 置换表 | 迭代加深 + 置换表(2^16) + 杀手×2 + 历史启发 |
| 7 | `KILLER_HISTORY` | 搜索类 | 杀手/历史启发 | αβ + 杀手槽×2 + 历史启发 |
| 8 | `VCF` | 威胁搜索 | 连续冲四（VCF） | 只用"逼着"（落子成四/成五）连击算杀 |
| 9 | `VCT` | 威胁搜索 | 连续威胁（VCT） | 逼着 + 活三，连续威胁算杀 |
| 10 | `KILL_SEARCH` | 威胁搜索 | 算杀 | 先 VCF 再 VCT；无杀回退普通 αβ 搜索 |
| 11 | `SHAPE_SCORE` | 评估类 | 棋型评分（经典） | 棋型评估 + αβ（**默认算法，行为冻结**） |
| 12 | `PATTERN_TABLE` | 评估类 | 模式匹配 | 18bit 线段查表评估（2^18 表，惰性构建） |
| 13 | `NEURAL_EVAL` | 评估类 | 神经网络评估 | 手写 3 层 MLP（20 维特征 → 32 tanh → 1 tanh，705 参数） |
| 14 | `MCTS` | 随机模拟 | 蒙特卡洛树搜索 | UCB1 树 + 均匀随机 rollout |
| 15 | `UCT` | 随机模拟 | 信心上界树（UCT） | UCB1 + 启发式 rollout |
| 16 | `RAVE` | 随机模拟 | 快速行动值估计（RAVE） | UCB1 混入 AMAF 胜率 |
| 17 | `TD_LEARNING` | 学习类 | TD 学习 | TD(0) + 20 维线性价值函数；权重持久化 |
| 18 | `Q_LEARNING` | 学习类 | Q 学习（强化学习） | 线性 Q(s,a) + ε-greedy；权重持久化 |
| 19 | `ALPHA_ZERO` | 学习类 | AlphaZero | PUCT MCTS + 手写策略/价值网络（962 参数）；无 rollout |
| 20 | `GENETIC` | 学习类 | 遗传算法 | 22 维基因组演化（锦标赛+交叉+高斯变异），每手至多一代 |
| 21 | `GREEDY` | 其他 | 贪心 | 落点收益 + 对手收益×0.9，无前瞻 |
| 22 | `FUZZY` | 其他 | 模糊推理 | 对数归一化 → 三角隶属度 → 4 条 T-Sugeno 规则加权 |
| 23 | `EXPERT_SYSTEM` | 其他 | 专家系统 | 8 条有序产生式规则链，首条非空者胜 |
| 24 | `ANT_COLONY` | 其他 | 蚁群算法 | 信息素跨手累积，τ^α·η^β 轮盘选点 + 蒸发/沉积 |
| 25 | `SIMULATED_ANNEALING` | 其他 | 模拟退火 | Metropolis 接受 + 降温 T_k = T0·0.92^k |

**家族划分（UI 分组顺序）**：搜索类（7）/ 威胁搜索（3）/ 评估类（3）/ 随机模拟（3）/ 学习类（4）/ 其他（5）。

### 7.3 5 档难度与参数

#### 7.3.1 难度原始参数（5 档）

| 档位 | 界面名 | depth（搜索层数，0=不搜索） | breadth（候选上限） | radius（邻域半径） | noise（劣着概率%） | alertPercent（战术留意率%） |
|---|---|---|---|---|---|---|
| `NOVICE` | 入门 | 0 | 8 | 1 | 85 | 30 |
| `EASY` | 简单 | 0 | 8 | 1 | 70 | 60 |
| `MEDIUM`（默认） | 中等 | 2 | 12 | 2 | 0 | 100 |
| `HARD` | 困难 | 4 | 16 | 2 | 0 | 100 |
| `MASTER` | 大师 | 6 | 12 | 2 | 0 | 100 |

> 注意：大师的 breadth(12) 小于困难(16)，靠更深搜索取胜；这是刻意的档位设计。

**全局硬上限**：时间预算 `[80, 150, 300, 600, 1200] ms`（入门→大师），任何引擎不得超过 1200ms；搜索层数上限 32。

**难度影响行棋强度的 9 条路径**：
1. 战术留意率 alertPercent（入门 30% / 简单 60% / 其余 100%，看漏时跳过战术底线判定）；
2. 劣着注入 noisePercent（入门/简单在候选前 5 名内随机换点）；
3. 搜索深度 depth；
4. 候选宽度 breadth；
5. 邻域半径 radius；
6. 每手时间预算；
7. 模拟数/迭代数/蚂蚁数/前瞻宽度等随档位单调递增；
8. 探索率递减（Q 学习 ε 40%→8%；AlphaZero 模拟数 40→320）；
9. 学习类强度主要来自累计权重，学习率与折扣不随档位变。

#### 7.3.2 各家族派生参数（档位 → 引擎参数）

**搜索类（7 预设）**：结构性开关按算法固定，档位只调深度与预算。
- 深度：`MINIMAX` 取 `[1,1,2,3,4]`、宽度恒 ≤8、节点上限 60,000；`ALPHA_BETA/PVS/MTD_F/KILLER_HISTORY` 取 `max(2, depth)`；`ITERATIVE_DEEPENING/TRANSPOSITION_TABLE/PATTERN_TABLE/NEURAL_EVAL` 取 `max(3, depth)`。
- MTD(f) 额外：置换表 16 位 + 零窗轮数 `4 + 档位序号`；迭代加深额外：渴望窗口；置换表预设额外：TT + 杀手×2 + 历史启发；非剪枝引擎（MINIMAX）节点上限 60,000，其余默认 200,000。

**威胁搜索（VCF/VCT/算杀）**：深度 = `max(2, depth+2)`、宽度 = `min(breadth, 12)`、半径 = `max(2, radius)`、时间预算 = 档位预算的一半（40/75/150/300/600ms）、节点上限 150,000。深度按"攻击方手数"计，防守方被迫应手不计深。

**随机模拟**：

| 项目 | 入门 | 简单 | 中等 | 困难 | 大师 |
|---|---|---|---|---|---|
| MCTS 模拟次数 | 120 | 240 | 500 | 900 | 1400 |
| UCT / RAVE 模拟次数 | 60 | 120 | 260 | 450 | 700 |

- MCTS：探索常数 1.4 + 均匀随机 rollout + 最大 rollout 深度 24；
- UCT：探索常数 1.2 + 启发式 rollout + 深度 16；
- RAVE：探索常数 1.2 + raveK=800 + 启发式 rollout + 深度 16；
- 三者共用：根候选宽度 = 档位 breadth、半径 = `max(2, radius)`；**模拟次数与时间预算先到者收口**。

**学习类**：浅搜深度 `[1,1,2,2,3]`；TD 学习率 0.25；Q 学习学习率 0.2、ε `[40,30,20,12,8]%`；AlphaZero PUCT c=1.5、模拟数 `[40,70,120,200,320]`；遗传算法进化预算固定 150ms（不随档位变）。折扣 γ=0.95。

**其他（5 个小引擎）**：专家系统前瞻宽度 `[3,4,6,8,10]`；蚁群蚂蚁数 `[4,6,10,16,24]`（α=1.0、β=2.0、蒸发率 0.3）；模拟退火迭代数 `[40,80,150,260,400]`（初温 3.0×活三分值、降温 0.92）；贪心与模糊推理无专属参数（共用 breadth/radius/预算）。

### 7.4 AI 引擎公共骨架

> 所有引擎（除行为冻结的 SHAPE_SCORE 外）继承统一骨架，子类只实现 `decide()`。**战术底线不可省**，否则弱档会"送人头"。

**引擎统一接口**：
- `var level: Difficulty`（思考中改档不得影响本次搜索 —— 入口处快照）；
- `getPosition(board: Array<IntArray>): Point` —— **实现必须先深拷贝入参，绝不修改调用方棋盘**；
- `onGameReset()`（默认空）、`onGameOver(outcome)`（默认空，outcome 含 winner=null 表示和棋）；
- **同一实例不可并发调用**（内部状态非线程安全）。

**`getPosition` 固定流程**：

1. 深拷贝棋盘；
2. 快照档位参数（alert/noise/breadth/radius/时间预算）；
3. 生成候选点（邻域内有子的空点，按棋型分排序，截断 breadth）；
4. 掷骰 `random < alertPercent` 时执行**战术预判**（见下），命中则直接取该点；
5. 否则调 `decide()` 选点，再按 noisePercent 概率注入劣着（在候选前 5 名内换点）；
6. 记录学习轨迹（学习类，落子前）；
7. 合法性兜底：非法 → 首个空候选 → 全盘首个空位 → 棋盘中心。

**战术预判（底线，优先级严格固定）**：
1. **我方连五**（按候选顺序找能立即连五的点）；
2. **堵对方连五**；
3. **我方形成活四**（对手堵不住，必胜）。

> 设计原则：弱档可以"看漏"（由 alertPercent 决定是否调用），但一旦调用就必须走对。

**候选点与评估基线**：
- 候选 = 空点且 radius 邻域内有子；打分 = 己方点位分 + 对方点位分 − 到中心曼哈顿距离；空盘返回中心单点。
- 排序必须**全序**（分数降序、x 升序、y 升序）—— 否则同种子不可复现。
- 棋型分值（全局量级基准，禁止改动数值）：五 1e8、活四 1e7、冲四 1e6、活三 2e5、眠三 2e4、活二 5e3、眠二 5e2、活一 1e2、眠一 1e1；胜分 = 五×10 = 1e9；评估返回 `己方 − 对方 − 对方/10`（1.1 倍防守偏置）。

### 7.5 各家族实现规格

| 家族 | 实现要点 |
|---|---|
| **搜索类** | 负极大 + 可插拔剪枝（NONE/αβ/PVS）；落子即连五返回 `±(胜分 − 层数)`（越早赢分越高）；节点上限与时间预算双保险（每 1024 节点检查一次，超限中止但保留已找到的根着法）；MTD(f) 零窗迭代；迭代加深逐层加深（渴望窗口，中断层丢弃）；置换表 2^16 定长单槽（同代不覆盖更深条目，杀棋分不入表）；着法排序 = 置换表最佳 → 杀手槽×2 → 历史启发 → 静态棋型分（末级按 x/y 升序保证全序） |
| **评估类** | 与"置换表"预设共用搜索骨架，仅替换评估函数：① 棋型（经典）为行为冻结的独立引擎；② 模式匹配：以每子为中心取 9 格×2bit 拼 18bit 索引查 2^18 表（**惰性建表**，首次评估时构建）；③ 神经网络：20 维特征 → 32 tanh 隐层 → 1 tanh 输出，705 参数，固定种子初始化（权重表只建一次、不参与学习），输出乘 `胜分/2` 并钳制，**绝不产生接近连五的终局分**（否则搜索会误判必胜） |
| **威胁搜索** | 迭代加深 1..depth，第一次找到的杀即最短杀；攻击方连五 → 对手连五反杀 → 唯一堵点须"挡的同时成四"才继续 → 遍历逼着（VCF）/逼着+活三（VCT/KILL）；防守方被迫应手不消耗攻击方手数；"活三防御点全部防住"才算攻击失败；节点上限 150,000 + 时间预算；**无杀时回退普通 αβ 搜索** |
| **随机模拟** | 树：选择（UCB1，RAVE 时混入 AMAF：β=√(k/(3N+k))）→ 展开 → rollout → 回传；胜率口径为"走出该着法一方"的胜场；启发式 rollout 前 10 步从静态分前 4 名随机取点，之后纯随机；终局取访问次数最多的根子节点；每 32 次模拟检查时间预算 |
| **学习类** | 见 §7.6 |
| **其他** | 贪心：`收益(己) + 收益(敌)×0.9`，并列取近中心；模糊：对数归一化 + 三角隶属度 + 4 条规则加权平均（确定性）；专家系统：8 条有序规则（自己连五→堵对方连五→自己活四→反双三→造双三→堵活三→一层前瞻→贪心中心），首条非空者胜，一层前瞻记"宁让一子不让一先"（应答威胁权重 1.2）；蚁群：信息素跨手累积、每手蒸发+沉积、轮盘选点；退火：候选表邻域 ±1 跳动、Metropolis 接受、返回历史最优 |

### 7.6 学习类引擎与权重持久化

- **模型 ID = 算法枚举名**（与难度无关：同一算法跨档位共享一份权重）。
- **惰性加载**：引擎首次 `getPosition` 或 `onGameOver` 时从存储读取一次；尺寸不符/数据损坏 → 视为无历史权重，用先验冷启动。
- **写库时机**：TD/Q/AlphaZero 每次落子后（有更新时）；遗传算法每完成一代；终局 `onGameOver` 后统一 flush。仅"脏"时写。
- **终局学习目标**：AI 胜 = +1（正向）、AI 负 = −1、和棋 = 0（AI 永远执白）。
- **新开/悔棋**：通知 `onGameReset()` —— TD/Q 丢弃未闭合轨迹，AlphaZero 清空样本缓冲，遗传只重置轨迹状态，蚁群复位信息素。
- **存储格式**：权重为 `DoubleArray`，编码为小端序、每权重 8 字节；解码时字节数非 8 倍数/为空 → 返回 null（损坏即无历史）。
- **存储接口是阻塞式的**，只允许后台线程调用（Android 侧为 Room 单表 `id TEXT 主键 + weights BLOB`）。
- **引擎池缓存策略**：只缓存学习类引擎（切走再切回不丢已学权重、进程重启后从存储重载）；非学习类每次获取都新建。

### 7.7 确定性与线程约束

| 约束 | 要求 |
|---|---|
| 随机源 | 必须注入（默认系统随机）；引擎池按 `种子 xor 算法序号` 为每个算法派生独立随机流 |
| 时钟 | 时间预算用注入的时钟（纳秒）；**预算 = 0 视为关闭**（测试传 `{ 0L }` 即可复现） |
| 排序全序 | 候选排序、着法排序末级都要有确定性决胜键 |
| 零副作用 | 引擎绝不修改传入棋盘 |
| 构造 O(1) | 大表一律惰性构建（2^18 模式表、神经网络权重表），构造可在主线程调用 |
| 并发 | 同一引擎实例禁止并发调用；搜索、学习钩子、权重落库共用**同一条单线程调度**（Android 侧 `Dispatchers.Default.limitedParallelism(1)`） |

---

## 8. 每步限时倒计时

| 项 | 需求 |
|---|---|
| FR-TIMER-1 时长 | 每步限时 **3 分钟（180,000ms）**，每秒 tick |
| FR-TIMER-2 适用范围 | 本地双人、局域网、蓝牙；**人机模式不适用** |
| FR-TIMER-3 展示 | `mm:ss`（固定 Locale 防本地化数字破坏等宽对齐）；未连接/未开始显示 `--:--`；等宽字体 |
| FR-TIMER-4 告警 | 剩余 < 30 秒时文字变 error 色，恢复后回常态色 |
| FR-TIMER-5 重置 | 成功落子、悔棋成功、重开 → 重置为 3 分钟；**非法操作不重置**（防乱点续命） |
| FR-TIMER-6 停表 | 任意终局（五连/和棋/认输/超时）与断线 |
| FR-TIMER-7 超时判负 | **只有"轮到自己走棋"的一方才判负**，胜方为其对手 |
| FR-TIMER-8 实现 | 计数式递减（每 tick 减 1000ms），不用墙钟差值（保证测试可虚拟时间） |
| FR-TIMER-9 联机开表 | 两端在**握手成功（连接建立）后**才起表，保证起点一致（握手最长可达数十秒，不得提前开表） |

---

## 9. 局域网联机

### 9.1 有线协议（字节冻结）

> **协议总纲**（任何实现必须逐字遵循）：
> - 所有长度前缀为 **1 字节无符号**；所有多字节量都是单字节，无字节序问题。
> - 字符串一律 **UTF-8**，每个字段原始字节 ≤255。
> - 解码器要求**长度精确匹配**，有多余字节即判畸形并丢弃该包（不崩溃、不断连）。
> - **只允许追加新的消息类型字节**，禁止修改既有字节值（新旧版本互通的前提）。

**端口与地址常量**

| 名称 | 值 |
|---|---|
| UDP 组播地址/端口 | `230.0.2.2 : 1688`（TTL=1，仅本网段） |
| UDP 单播端口 | `2599`（本机绑定端口兼目的端口） |
| TCP 对局端口 | `8899` |
| UDP 缓冲 | 1024 字节 |
| TCP 读缓冲 | 2048 字节（非帧上限） |
| TCP payload 上限 | 125 字节（编码侧断言） |
| 蓝牙（参考） | UUID `00001101-0000-1000-8000-00805F9B34FB`，服务名 `Gomoku` |

#### 9.1.1 UDP 组播发现帧（JOIN / EXIT）

布局：`[nameLen][name][ipLen][ip][type]`，type 为**末字节**；目标 `230.0.2.2:1688`。

| 偏移 | 长度 | 字段 | 含义 |
|---|---|---|---|
| 0 | 1 | nameLen = N | 设备名 UTF-8 字节数 |
| 1 | N | name | 设备显示名（Android 为 `品牌-型号`，如 `HUAWEI-P40`） |
| 1+N | 1 | ipLen = M | 自报 IP 字节数 |
| 2+N | M | ip | 自报 IP（如 `192.168.1.23`） |
| 2+N+M | 1 | type | `0=JOIN`，`1=EXIT` |

- **接收侧必须以 UDP 传输层源地址为准**（自报 IP 可能陈旧/错误，仅当源地址为空时兜底使用）；源 IP 等于本机则丢弃（自过滤）。
- **golden 向量（可作测试用例）**：`JOIN("HUAWEI-P40","192.168.0.7")` → `0A 'HUAWEI-P40' 0B '192.168.0.7' 00`，共 24 字节；旧版 `EXIT("OPPO-Reno","10.0.0.3")` → `08 'OPPO-Reno' 07 '10.0.0.3' 01` 必须可解析。

#### 9.1.2 UDP 单播信令帧

布局：`[type][nameLen][name][ipLen][ip]`（与组播帧同布局，仅 type 移到首字节）；目的 `对端IP:2599`。

| 字节 | 类型 | 方向 | 语义 |
|---|---|---|---|
| `0` | UDP_JOIN | 应答方 → JOIN 源 | "我在线"（对组播 JOIN 的应答） |
| `11` | ASK | 发起方 → 目标 | 请求联机 |
| `12` | AGREE | 被请求方 → 发起方 | 同意联机 |
| `13` | REJECT | 被请求方 → 发起方 | 拒绝（用户拒绝 / 正忙 / 已终局） |
| `14` | CHAT | 双向 | 聊天（追加字段见下） |

- 收到组播 `JOIN` → **必须回一个单播 UDP_JOIN 到「源IP:2599」**，让对方也把自己入列；收到 `EXIT` → **不应答**。
- `AGREE`/`REJECT` 只被"正在等待的同一 IP"接受，迟到/错配包直接丢弃，不回发任何东西。
- 未知 type 字节：静默忽略。

#### 9.1.3 CHAT 帧

布局：`[14][nameLen][name][ipLen][ip][chatLen][chat]`，chat ≤255 字节 UTF-8，总长 = N+M+C+4。

#### 9.1.4 TCP 对局帧

布局：`[len][type][payload]`；`len` = **含 len 与 type 两个字节的整帧总长**（`len = 2 + payload.size`）；无分隔符，靠 len 分帧。

| 字节 | 类型 | 发送方 | payload | 语义 |
|---|---|---|---|---|
| `0` | ADD_CHESS | 落子方 | `[x][y]`（0 基坐标） | 落子（接收方以对端颜色落子） |
| `1` | CONNECTED | — | — | **保留未使用**（收到应忽略） |
| `2` | ROLLBACK_ASK | 请求方 | 空 | 请求悔棋 |
| `3` | ROLLBACK_AGREE | 被请求方 | 空 | 同意悔棋 |
| `4` | ROLLBACK_REJECT | 被请求方 | 空 | 拒绝悔棋 |
| `5` | RESTART | 任一方 | 空 | 同步重开（无需确认） |
| `6` | DRAW_ASK | 发起方 | 空 | 请求和棋 |
| `7` | DRAW_AGREE | 被请求方 | 空 | 同意和棋 |
| `8` | DRAW_REJECT | 被请求方 | 空 | 拒绝和棋 |
| `9` | RESIGN | 认输方 | 空 | 单方宣告认输（对方胜） |
| `10` | TIMEOUT | 超时方 | 空 | 单方宣告每步限时归零判负；**要求两端同版本** |
| `11` | HEARTBEAT | 双向 | 空 | 心跳保活/假死检测（**追加类型**：旧版收到按未知类型忽略，检测见 §9.1.5） |

**golden 帧**：`ADD_CHESS(7,8)` → `04 00 07 08`；`ROLLBACK_ASK` → `02 02`；`RESTART` → `02 05`；`DRAW_ASK` → `02 06`；`DRAW_AGREE` → `02 07`；`DRAW_REJECT` → `02 08`；`RESIGN` → `02 09`；`TIMEOUT` → `02 0A`；`HEARTBEAT` → `02 0B`。

**分帧器（TcpFrameReader）状态机**（粘包/半帧安全）：
1. 新字节追加到持久缓冲尾部；
2. 循环：读首字节 `frameLen`；**若 <2：丢弃 1 字节继续**（逐字节重同步，防死循环）；若剩余不足 frameLen：等待更多字节；否则产出一帧（type + payload）并前进 frameLen；
3. 消费完的前缀丢弃，保留半帧尾巴；
4. 未知 type 的帧仍产出、由调用方忽略（保证追加类型的向前兼容）。

#### 9.1.5 传输与取消机制

- 全部 IO 在后台线程的阻塞循环中执行；**`receive()`/`read()`/`accept()` 不响应协程/线程取消** —— 停止顺序固定：置 running=false → **关闭 socket**（打断阻塞）→ 取消任务。
- 读循环：UDP 的 `SocketException` → 正常退出；其他 IO 异常 → 记日志 + 间隔 200ms 重试（瞬时错误不得永久退出）；每次 receive 前重置包长度参数（复用缓冲的坑）。
- TCP 读返回 -1 或抛异常 → **仅当 running 仍为 true** 才上报断线（本地主动退出不误报）。
- 发送需串行化（互斥锁），发送失败只记日志，由读循环负责上报断线。
- **心跳与假死检测**（局域网/蓝牙共用同一实现）：连接成功后每 **5 秒**发送一个 `HEARTBEAT`（空 payload，复用发送串行化通道）；任意收到的帧都会刷新"最后收帧时间"。
  - **旧版互通 gate**：只有**收到过对端心跳**之后才启用判死 —— 与旧版对局时对端从不发心跳，本端只发不判，不会误报断线。
  - 判死条件：收到过对端心跳 && 距最后收帧 ≥ **15 秒** → 视为假死（对端崩溃/网络静默丢失），走既有 `Disconnected` 事件路径（停表 → Toast `对方已断开连接` → 退出对局页）。检测窗口 ≤ 20 秒。
  - 真断线（对端发 FIN/RST）仍由读循环上报，不依赖心跳。

### 9.2 发现与信令

| 项 | 需求 |
|---|---|
| FR-LAN-1 进入条件 | 进入联机页先取本机 WiFi IP；WiFi 未开或 IP 为空 → Toast `请检查 WiFi 连接后重试` → 退出页面 |
| FR-LAN-2 自动发现 | 进入页面即自动发送一次 JOIN 广播；同时监听组播 1688 与单播 2599 |
| FR-LAN-3 设备列表 | 主文案 `玩家<设备名>`，副文案 `IP地址<IP>`；**按 IP 去重**；同名重复刷新**原地更新、不移动位置**（防列表抖动），改名才移到末尾；设备名与 IP 相同时隐藏第二行 |
| FR-LAN-4 手动扫描 | 点「局域网联机扫描」：**先清空列表**再重发 JOIN（在线设备会回 UDP_JOIN 重建列表）→ Toast `扫描中，请稍后......` |
| FR-LAN-5 离线清理 | 联机页每 **10 秒**重播一次 JOIN 广播（在线设备借此刷新最后在线时间）；超过 **30 秒**未被刷新（约 3 个周期）即自动从列表淘汰 —— 对端崩溃/掉线不发 EXIT 也能清理。收到 EXIT 仍即时移除；手动重扫同步清空时间戳 |
| FR-LAN-6 发起请求 | 点设备行 → 发 ASK → 弹**可取消**等待框 `等待 %1$s 回应，请稍后…`（含对端 IP）→ 启动 **10 秒**超时 |
| FR-LAN-7 发起超时 | 10 秒无回应 → 收等待框 → Toast `对方无响应，请稍后再试` |
| FR-LAN-8 收到拒绝 | 收等待框 → Toast `对方拒绝了你的请求` |
| FR-LAN-9 收到同意 | 收等待框 → 进入对局页（client 身份） |
| FR-LAN-10 用户取消等待 | 仅本地复位（**不发任何取消消息**，对端弹窗仍可能后续回包被忽略） |
| FR-LAN-11 被请求弹窗 | 收到 ASK → **不可取消**对话框 `「<对端名>请求和你对战」` + `同意`/`拒绝` |
| FR-LAN-12 忙则拒绝 | 已有待裁决弹窗或正在等待他人回应时，对后来的 ASK **立即回 REJECT** |
| FR-LAN-13 互戳仲裁 | 双方同时向对方发起（本端在等待中且 pending IP == 对方 IP）：按**本机 IP < 对端 IP 的字符串字典序**裁定 —— 成立方自动同意并进游戏（server），不成立方继续等待对方回包（不回 REJECT） |
| FR-LAN-14 对端退出 | 收到 EXIT：若正等待该 IP 或它的请求弹窗开着 → 关闭并 Toast `对方已退出` |
| FR-LAN-15 离开页面 | 退出联机页时发送 EXIT 组播（用临时 socket 发送，避免与关闭时序竞争） |

### 9.3 建连与角色

| 项 | 需求 |
|---|---|
| FR-LAN-20 角色 | **被请求方（同意方）= server = 执黑先手**；发起方 = client = 执白 |
| FR-LAN-21 建连 | 进入对局页即开始：server 监听 8899，`accept` 超时 **30 秒**（仅一次，不循环）；client 重试连接 **10 次 ×间隔 250ms**（每次失败后都等待，含最后一次），全失败 → 建连失败 |
| FR-LAN-22 等待提示 | 进入对局页弹**可取消**等待框 `建立连接中，请稍后`；连接成功自动收起并**此刻开表**倒计时 |
| FR-LAN-23 失败 | 建连失败 → 收等待框 → Toast `建立连接失败,请重试` → 退出对局页 |
| FR-LAN-24 断线 | 连接断开 → 停表、收等待框 → Toast `对方已断开连接` → 退出对局页 |
| FR-LAN-25 非法进入 | 缺少必要参数 → Toast `建立网络失败，请重试` → 退出 |

### 9.4 对局状态机

> 局域网与蓝牙**必须完全一致**（同一份对局逻辑实现）；差异仅在建连层。

**关键状态位**：`connected`、`declaredOver`（本层终局已宣告）、`awaitingRollbackResponse` / `rollbackDialogShowing`、`awaitingDrawResponse` / `drawDialogShowing`、黑白胜场计数（仅内存）。

| 功能 | 需求 |
|---|---|
| FR-NET-1 落子 | 前置：已连接、未终局、无待决悔棋、无弹窗、轮到自己。**制胜一手也必须发送**（否则对端收不到终局）；成功落子作废本方待决求和；对端落子以**对端颜色**直接应用（不校验轮次），并重置倒计时 |
| FR-NET-2 悔棋 | 协商制：请求方有本方棋子**且未终局**才可发起（发送后无弹窗仅等待）；被请求方弹**不可取消**框 `是否同意对方悔棋`；同意 → 回 AGREE 并**立即**执行回退；拒绝 → 回 REJECT |
| FR-NET-3 悔棋回退算法 | **双端各自从同一份落子历史推导**：`indexOfLast { 属于请求方 }` → 移除"请求方最后一手及其之后的所有棋子"（可能多于一子）。**禁止"各删一子"或"只删最后一手"**（旧版失同步缺陷） |
| FR-NET-4 悔棋提示 | 请求方收到同意 → Toast `对方同意悔棋`；收到拒绝 → Toast `对方拒绝了你的请求`；回退后重置倒计时 |
| FR-NET-5 重开 | 单方宣告：本地立即清盘 + 发 RESTART；对端收到清盘并 Toast `对方已重新开始`；比分不清零 |
| FR-NET-6 求和 | 协商制：点求和 → 发 DRAW_ASK + Toast `已发送求和请求`；对端弹**不可取消**框 `对方请求和棋，是否同意？`；同意 → 双方横幅 `双方和棋`（**双方胜场均不加**）、停表；拒绝 → 请求方 Toast `对方拒绝求和` |
| FR-NET-7 认输 | 单方宣告：点认输先弹确认框 `确定要认输吗？本方判负`；确定 → 发 RESIGN + 本地宣告对方胜（双方各自从同一事件推导，胜场同步 +1） |
| FR-NET-8 超时 | 3 分钟归零且轮到自己 → 本地宣告自己判负 + 发 TIMEOUT；对端收到 → 判自己获胜，**绝不回发**；不轮到自己则保持沉默（不发任何包）。任一时刻至多一个发送方 |
| FR-NET-9 终局压制 | 终局（或引擎已 over）后：本方**禁发**悔棋/求和/落子；收到的新悔棋/求和请求一律不弹窗、**必须回执**（悔棋回 ROLLBACK_REJECT、求和回 DRAW_REJECT）；已开弹窗被作废并关闭；若在弹窗关闭前的竞态里点了"同意"，也只回 REJECT、**绝不回退**（不回退才能保住终局结果）。**任何协商请求都必须有回执**：本端"忙"（已开同类弹窗或正在等待回应）时同样立即回 REJECT，禁止静默丢包（否则对端永久悬挂、此后无法落子） |
| FR-NET-10 结果横幅 | 本方胜 `恭喜你！你赢了！`；本方负 `很遗憾！你输了！`；和棋 `双方和棋`；超时本方胜 `对方超时判负，你赢了！`；超时本方负 `你超时判负，很遗憾！` |
| FR-NET-11 记分 | 仅会话内内存，退出页面清零；**悔棋不回收比分**（解除终局后胜场保持，与本地双人/人机口径一致） |
| FR-NET-12 页面元素 | 记分卡（自己/对手，按身份定左右）+ 倒计时 + 终局横幅 + 棋盘 + 按钮「新开」「悔棋」「求和」「认输」（认输为 error 色危险按钮） |
| FR-NET-13 按钮可用性 | 按钮始终可见可点；非法/不满足前置的点击静默忽略（不置灰） |

### 9.5 聊天

- 传输：UDP 单播 2599（§9.1.3），**与对局无关，仅存在于联机页**。
- 接收：收到 CHAT → 弹「对话」对话框（列表）；消息署名 `对端名(对端IP)`、时间（接收方本机时间，格式 `yyyy-M-d HH:mm:ss`）、内容；**最新消息在最上方**。
- 发送：对话对话框底部为输入框 + 发送按钮（软键盘动作同为发送）；**回复对象 = 最近一条收到的消息的对端 IP**（联机页没有"当前会话"概念），无来信记录时发送无效；本机发出的消息立即在本地回显，署名 `自己(本机IP)`。超过 255 字节按 UTF-8 字符边界自动截断（不劈开多字节字符）。
- 限制：消息 ≤255 UTF-8 字节；无已读/未读、无持久化、无重传、无历史（关闭对话框即丢）；蓝牙模式无聊天。

---

## 10. 蓝牙联机

> 蓝牙没有组播信令：**联机请求本身就是一条 RFCOMM 连接**。控制通道与对局通道都用同一个服务 UUID。

### 10.1 前置就绪顺序（严格顺序，任一失败即提示并退出）

1. 设备不支持蓝牙（无蓝牙硬件）→ Toast `本机不支持蓝牙` → 退出；
2. Android 12+ 申请运行时权限 `BLUETOOTH_CONNECT` + `BLUETOOTH_SCAN`（<12 无运行时权限）：拒绝 → Toast `需要蓝牙权限才能联机` → 退出；
3. 蓝牙未开启 → 拉起系统"开启蓝牙"对话框：未开启 → Toast `请先开启蓝牙后重试` → 退出；
4. **以上三步全部就绪后**才创建页面状态机（开始监听 + 播报已配对设备）。用一次性标志防重复初始化。

### 10.2 页面与列表

| 项 | 需求 |
|---|---|
| FR-BT-1 本机身份条 | 顶部显示 `本机：%1$s`（拿到本地地址时 `本机：%1$s（%2$s）`）；名称取不到显示 `未命名设备`；文字可选中。**不将本机蓝牙地址视为必然可得**（Android 6+ 返回占位值、11+ 需要系统权限；禁止反射绕过）；局域网页不显示此条 |
| FR-BT-2 说明与按钮 | 说明 `搜索已配对或附近的蓝牙设备`；按钮 `蓝牙联机扫描` |
| FR-BT-3 列表 | 主文案 `玩家<设备名>`、副文案 `MAC地址<地址>`；按 MAC 去重；设备名取不到时回退为 MAC 且隐藏第二行 |
| FR-BT-4 初始列表 | 进入即播报**已配对设备**（蓝牙最主要的发现来源，页面一进来就有内容），UI 不区分"已配对/发现中" |
| FR-BT-5 扫描 | 点扫描：清空列表 → **重新播报已配对设备** → 取消系统发现并重新开始系统发现（附近未配对设备以系统广播异步出现；扫描期间新配对成功的设备也即时入列）→ Toast `扫描中，请稍后......` |
| FR-BT-6 权限提示 | 扫描/连接/列设备过程中缺权限或蓝牙被关闭 → 对应 Toast（见 §12 错误码文案） |

### 10.3 握手（被叫方）

1. 页面就绪后常驻监听：RFCOMM 服务记录（服务名 `Gomoku` + 标准 SPP UUID）。**accept 返回即视为收到联机请求**（无应用层字节交互）。
2. 收到请求即弹**不可取消**对话框 `「<对端名>请求和你对战」` + `同意`/`拒绝`；此时若本地正忙（等待他人回应或已有弹窗）→ **立即回 REJECT**；若已有一条待裁决连接再来新连接 → 也立即回 REJECT（同一时刻最多一个待裁决连接）。
3. 裁决回写**恰好 1 个字节**：同意 `20`、拒绝 `21`（`BtSignal`）。
4. **同意路径的顺序不可动**：
   1. 先停止监听（让出服务 UUID）；
   2. 再回写 AGREE（写完后才关闭该控制连接）；
   3. 最后释放管理器并进入对局页（server，执黑）。
   > 反例后果：若先回 AGREE 再关监听口，主叫可能在收到 AGREE 后立刻连上"即将关闭的旧监听口"，随即被判断线。
5. 拒绝后**继续监听**（可再接新请求）。

### 10.4 握手（主叫方）

| 项 | 需求 |
|---|---|
| FR-BT-10 前置 | 发起连接前**必须取消系统发现**（发现过程会显著拖慢甚至阻塞 RFCOMM 建连） |
| FR-BT-11 等待提示 | 弹**可取消**等待框 `等待对方回应，请稍后…`（蓝牙版**不含对端地址**，与局域网不同） |
| FR-BT-12 裁决读取 | 阻塞读 1 字节；**20 秒看门狗**：超时关 socket 打断阻塞读（对端闪退不能永久挂住） |
| FR-BT-13 结果处理 | 读到 `20` → 释放管理器 → 进对局页（client，执白）；读到 `21` 或连接被直接关闭（读得 −1）→ **一律按拒绝处理**，Toast `对方拒绝了你的请求` |
| FR-BT-14 页面超时 | 20 秒仍未裁决 → 收等待框 → Toast `对方无响应，请确认对方已进入蓝牙联机页面` |
| FR-BT-15 管理错误 | 发生蓝牙错误（未开启/缺权限/建连失败）→ **先收起等待框**再 Toast（否则提示被对话框盖住） |

### 10.5 对局通道

| 项 | 需求 |
|---|---|
| FR-BT-20 建连 | 两端用**同一个服务名 + UUID** 重新建连（对端同意后有"关旧监听口→开新监听口"的短暂空窗，所以必须重试）：server `accept` 超时 **30 秒**；client 在 **15 秒墙钟预算**内每 **300ms** 重试一次（非固定次数 —— 单次 connect 可能耗时数十秒） |
| FR-BT-21 帧格式 | 与局域网**完全复用**：`[len][type][payload]` + 同一套分帧器；因此悔棋/重开/求和/认输/超时/心跳的语义自动一致（心跳 5s 发送 / 15s 判死，覆盖两种传输） |
| FR-BT-22 角色 | 同局域网：被请求方（server）执黑先手、发起方执白 |
| FR-BT-23 进入对局 | 联机页进入对局页后**自身出栈**（监听口已让出，页面不可复用，返回也点不动） |
| FR-BT-24 无聊天 | 蓝牙没有带外通道，无聊天功能 |
| FR-BT-25 对端退出事件 | 蓝牙没有"对端离开"广播，列表不自动移除（只能靠手动扫描清空） |

### 10.6 平台注意事项

- 系统广播接收器（设备发现、配对状态、蓝牙开关状态）来自**系统蓝牙栈进程**，注册时必须声明为**导出（EXPORTED）**（Android 14+ 用 NOT_EXPORTED 会静默收不到）。
- 蓝牙权限矩阵：`BLUETOOTH` / `BLUETOOTH_ADMIN`（≤API 30 安装时授予）；`BLUETOOTH_CONNECT` / `BLUETOOTH_SCAN`（API 31+ 运行时申请；SCAN 声明 `neverForLocation`，避免索要定位权限）。
- 无蓝牙硬件的设备可安装（feature 声明非必需），运行时兜底提示。

---

## 11. 数据与持久化

| 数据 | 存储 | 键/结构 | 时机 |
|---|---|---|---|
| AI 算法选择 | 本地键值（Android SharedPreferences `fivechess`） | `ai_algorithm_v1` = 枚举名字符串；读不到/不认识 → 默认 `SHAPE_SCORE` | ViewModel 构造时读；用户在对手设置页变更时立即写 |
| AI 难度 | 同一份键值 | `ai_level_v2` = 枚举名字符串；读不到/不认识 → 默认 `MEDIUM`；兼容迁移旧键 `ai_level`（Int：0=简单/1=中等/2=困难），读取后写新键并删旧键 | 同上 |
| AI 学习权重 | 嵌入式数据库（Android Room，库名 `ai_weights.db`，版本 1） | 表 `ai_weights`：`id TEXT 主键`（算法枚举名）+ `weights BLOB`（DoubleArray 小端序编码） | 首次用某学习算法时懒加载；逐手/每代/终局保存（仅脏时写） |
| 对局比分 | **不持久化** | 仅 ViewModel 内存 | 退出页面即清零 |
| 聊天记录 | **不持久化** | 仅内存、无上限 | 退出页面即丢 |
| 棋局过程 | **不持久化** | 无续局/存档功能 | — |

- 数据库不在启动时打开，进入人机对局页时才懒建。
- 备份：设置与学习权重属于用户数据，允许系统备份/设备迁移（Android 当前为默认全量备份）。

---

## 12. 全文案总表

> 复刻要求：所有文案必须逐字一致（含全角/半角标点、空格、省略号形式）。`…` 为单字符省略号。

### 12.1 通用

| 场景 | 文案 |
|---|---|
| App 名 | `五子棋` |
| 副标题 | `原木棋盘 · 墨绿对弈` |
| 黑/白 | `黑方` / `白方` |
| 新开 / 悔棋 | `新开` / `悔棋` |
| 求和 / 认输 | `求和` / `认输` |
| 确定 / 取消 / 同意 / 拒绝 | `确定` / `取消` / `同意` / `拒绝` |
| 自己 / 对手 / 电脑 | `自己` / `对手` / `电脑` |
| 玩家（列表前缀） | `玩家` |
| IP 地址 / MAC 地址标签 | `IP地址` / `MAC地址` |

### 12.2 首页 / 关于

| 场景 | 文案 |
|---|---|
| 模式副标题 | `25 种算法 × 5 档难度`、`同屏轮流落子`、`连接同一 WiFi 的玩家`、`连接附近的蓝牙设备玩家` |
| 关于标题 / 正文 / 按钮 | `关于` / `欢迎访问源代码\nhttps://github.com/lany192/FiveChess` / `确定` |

### 12.3 对手设置

| 场景 | 文案 |
|---|---|
| 标题 | `对手设置` |
| 小标题 | `难度` / `算法` |
| 难度名 | `入门` / `简单` / `中等` / `困难` / `大师` |
| 家族名 | `搜索类` / `威胁搜索` / `评估类` / `随机模拟` / `学习类` / `其他` |
| 对局页摘要 | `算法：%1$s · 难度：%2$s` |
| 算法名 25 条 | `极小极大`、`Alpha-Beta 剪枝`、`主要变例搜索`、`MTD(f) 零窗口迭代`、`迭代加深`、`置换表`、`杀手/历史启发`、`连续冲四（VCF）`、`连续威胁（VCT）`、`算杀`、`棋型评分（经典）`、`模式匹配`、`神经网络评估`、`蒙特卡洛树搜索`、`信心上界树（UCT）`、`快速行动值估计（RAVE）`、`TD 学习`、`Q 学习（强化学习）`、`AlphaZero`、`遗传算法`、`贪心`、`模糊推理`、`专家系统`、`蚁群算法`、`模拟退火` |

### 12.4 对局结果（横幅）

| 场景 | 文案 |
|---|---|
| 双人/人机 黑胜 | `黑方胜！` |
| 双人/人机 白胜 | `白方胜！` |
| 双人 黑超时 | `黑方超时判负，白方胜！` |
| 双人 白超时 | `白方超时判负，黑方胜！` |
| 双人/人机 和棋（满盘） | `双方和棋` |
| 联机 本方胜 | `恭喜你！你赢了！` |
| 联机 本方负 | `很遗憾！你输了！` |
| 联机 和棋 | `双方和棋` |
| 联机 对方超时 | `对方超时判负，你赢了！` |
| 联机 本方超时 | `你超时判负，很遗憾！` |

### 12.5 对话框

| 场景 | 标题 | 正文 | 按钮 | 可取消 |
|---|---|---|---|---|
| 收到联机请求（LAN/BT） | 无 | `<对端名>请求和你对战` | 同意 / 拒绝 | **否** |
| 发起联机等待（LAN） | 无 | `等待 %1$s 回应，请稍后…`（%1$s=IP） | 无 | 是（取消即本地复位） |
| 发起联机等待（BT） | 无 | `等待对方回应，请稍后…` | 无 | 是 |
| 对局页建立连接 | 无 | `建立连接中，请稍后` | 无 | 是 |
| 收到悔棋请求 | 无 | `是否同意对方悔棋` | 同意 / 拒绝 | **否** |
| 收到求和请求 | 无 | `对方请求和棋，是否同意？` | 同意 / 拒绝 | **否** |
| 认输确认 | 无 | `确定要认输吗？本方判负` | 确定 / 取消 | 是 |
| 聊天（LAN） | `对话` | 列表（最新在上）：`对端名(IP)` / 己方 `自己(本机IP)` + 时间 + 内容；底部输入框（提示 `输入消息`）+ `发送` 按钮 | 无 | 是 |

### 12.6 Toast / 提示

| 触发 | 文案 |
|---|---|
| 扫描中（LAN/BT 共用） | `扫描中，请稍后......` |
| LAN 发起超时 | `对方无响应，请稍后再试` |
| BT 发起超时 | `对方无响应，请确认对方已进入蓝牙联机页面` |
| 对方拒绝 / 悔棋被拒 | `对方拒绝了你的请求` |
| 对端离开联机页 | `对方已退出` |
| 对局页建连失败 | `建立连接失败,请重试`（注意：半角逗号） |
| 对局页断线 | `对方已断开连接` |
| 悔棋被同意（请求方） | `对方同意悔棋` |
| 求和已发送 | `已发送求和请求` |
| 求和被拒 | `对方拒绝求和` |
| 对方重开 | `对方已重新开始` |
| 非法进入对局页 | `建立网络失败，请重试` |
| WiFi 未开 | `请检查 WiFi 连接后重试` |
| 蓝牙未开 / 缺权限 / 不支持 | `请先开启蓝牙后重试` / `需要蓝牙权限才能联机` / `本机不支持蓝牙` |
| 蓝牙本机名未知 | `未命名设备` |
| 探知层错误码 | `网络套接字未就绪` / `本机IP获取失败` / `目标地址无效` / `数据发送失败` / `组播不可用` / `蓝牙未开启，请先打开蓝牙` / `缺少蓝牙权限，请授权后重试` / `蓝牙连接失败，请确认双方已配对并都在联机页面` |
| 聊天输入 | `输入消息`（输入框提示）/ `发送`（按钮） |

> 本表全部文案已收敛到 `strings.xml`（Effect 只携带 `@StringRes`，不再硬编码在 Kotlin）。取舍：release 的字符串加密（`EncryptStringsTask`）只覆盖代码常量池，**不覆盖资源文件** —— 移入 res 的这些文案在 release 产物中以明文存在（本身也非敏感信息）。

---

## 13. 平台差异与 iOS 移植要点

> 以下为移植建议，不是现有实现的事实，供 iOS 端设计参考。

| 主题 | Android 现状 | iOS 对应 |
|---|---|---|
| 蓝牙对局 | RFCOMM/SPP（服务名 + 标准 SPP UUID） | iOS **无公开 RFCOMM/SPP API**。要保留"请求即连接"语义需走 MFi/ExternalAccessory（需认证硬件，且不能与 Android 端互通），或改用 BLE GATT（Peripheral 广播自定义 Service / Central 连接后写 1 字节裁决）。**BLE 与 Android Classic BT 之间无法互通** —— 若要求双平台蓝牙互通，两侧协议需另行设计。**结论：iOS 与 Android 的蓝牙联机互通不可行**（平台限制，非协议问题；局域网联机不受影响），iOS 端的蓝牙对局只能作为同平台功能，或不做蓝牙模式 |
| 局域网 | UDP 组播 230.0.2.2:1688 + 单播 + TCP 直连 | 需 `NSLocalNetworkUsageDescription`（iOS 14+ 本地网络权限）与组播 entitlement（多播/广播需要申请 `com.apple.developer.networking.multicast`）；TCP/UDP API 用 Network.framework 即可，字节协议 100% 复用 |
| 设置存储 | SharedPreferences | UserDefaults（键名保持一致） |
| 权重存储 | Room 单表 BLOB | SQLite / CoreData 单表（id 主键 + BLOB），编码保持"小端序 Double 数组" |
| 权限矩阵 | 见 §10.6 | 蓝牙需要 `NSBluetoothAlwaysUsageDescription`；无 Android 的 31+ 版本分叉 |
| 全屏适配 | edge-to-edge + insets 叠加 | safeAreaInsets 叠加到根 padding |
| 文案 | `strings.xml`（已全量收敛，Kotlin 不再硬编码） | Localizable.strings 统一管理 |

**跨端必须逐字复刻**（互通与一致性红线）：
1. 三个端点与全部协议字节（含保留字节 `CONNECTED=1` 不得复用、只允许追加新类型）；
2. `len` 语义（含自身的整帧总长）与"长度精确匹配、畸形即丢包"、"len<2 逐字节重同步"；
3. 收到组播 JOIN 必须用单播回源 IP:2599；EXIT 不应答；
4. 角色约定（同意方=黑先手，发起方=白）；
5. 悔棋移除算法（请求方最后一手及其之后全部）与"终局不得因同意协商而复活"；
6. 超时只由"轮到自己且归零"的一方宣告、对方不回发；
7. 设备地址以传输层源为准（自报仅兜底）；
8. 停止阻塞 IO 一律"先关 socket 再取消任务"。

---

## 14. 非功能需求

| 类别 | 要求 |
|---|---|
| 架构（Android 侧） | 分层单向依赖：`domain/`（纯逻辑，禁平台 API、可 JVM 测试）← `data/` ← `ui/`；禁止反向依赖 |
| 状态管理 | 单向下行数据流（手写 MVI）：Intent 经**无限容量队列串行**处理；State 为可观察流、**渲染必须幂等**（旋转/重建后由状态重放恢复）；Effect 为一次性事件（不重放），订阅方须处于前台 |
| 依赖 | 无第三方 DI/网络/JSON 库；协议为自研字节编解码 |
| 性能 | AI 单步耗时 ≤ 1.2 秒硬上限；思考在后台线程，不阻塞 UI；引擎构造 O(1)、大表惰性构建 |
| 可测试性 | 随机与时钟可注入；棋盘拷贝零副作用；排序全序保证同种子可复现；规则引擎与协议编解码必须有单元测试（含字节冻结用例） |
| 稳定性 | 畸形网络包只丢弃不崩溃；空历史悔棋/非法落子安全无操作；广播接收器重复注销做防护 |
| 兼容性 | 协议只允许追加；新增消息类型默认对旧版静默忽略 |
| 使用体验 | 所有网络等待有等待框、所有失败有明确 Toast；无阻塞主线程操作 |

---

## 15. 验收标准

### 15.1 规则与棋盘

1. 15×15 交点落子，黑先；横/纵/斜（含反斜贴角）五连判胜并高亮完整连珠段；四子不判胜。
2. 六连判胜且高亮 6 点。
3. 越界/占用/非本方回合/终局后的落子被拒且无视觉反馈。
4. 悔棋能解除五连终局、超时终局与满盘和棋；空历史悔棋安全；悔棋不回退比分。
5. 快照与引擎内部隔离（外部修改快照不影响引擎）。
6. 满盘（225 手）无五连判和棋：`over=true`、`winner=null`、双方不加胜场；最后一手同时成五按胜局处理（五连优先）；和棋后落子被拒。

### 15.2 各模式

7. 双人：悔棋 1 步、比分保留、3 分钟限时、超时判负文案正确；满盘和棋显示 `双方和棋` 且停表。
8. 人机：人黑 AI 白；悔棋 2 步；思考中悔棋/换算法/重开按 §7.1 行为；换算法×5 档难度全部可玩且不崩溃；AI 落第 225 手判和棋并收到 `GameOutcome(null)` 学习钩子。
9. 联机：局域网与蓝牙的悔棋/求和/认输/重开/超时行为完全一致；悔棋后两端棋盘与轮次一致；求和两端同时显示和棋且双方比分不变；满盘第 225 手两端各自本地判和（无需新消息）。
10. 终局后：两端棋盘锁定；本方禁发悔棋/求和/落子；对收到的协商请求必须回执 REJECT（不静默丢包）。
11. 联机协商不会永久悬挂：任一端"忙"（弹窗挂着/等待回应）时对同类请求立即回 REJECT。

### 15.3 协议互通（关键）

12. 帧编解码对 golden 向量逐字节一致（含追加的 `HEARTBEAT` → `02 0B`）。
13. 半帧/粘包/畸形长度帧不阻塞后续帧解析。
14. 新旧版本混用：旧版收到 RESTART/DRAW_*/RESIGN/TIMEOUT/HEARTBEAT 等新字节时静默忽略（不崩溃、不断连）；新版与旧版对局**不会因心跳误判断线**（收到过对端心跳后才启用判死）。
15. 双端真机同网验证：发现列表、握手、对局、悔棋、求和、认输、超时、断线、心跳假死共 9 条链路。

### 15.4 AI

16. 25 种算法 × 5 档难度均可完成一局（无超时、无非法落点、无崩溃）。
17. 任一引擎在"我方/对方有连五"局面下（在 alertPercent 命中时）必取连五点/必堵；活三必成活四（适用档位）。
18. 入门/简单档存在可感知的失误（漏堵、劣着），且入门失误率高于简单。
19. 同种子同局面结果可复现（关闭时间预算时）。
20. 学习类权重跨会话保留：对局后重启进程，同算法加载既有权重；权重损坏（字节数非法）时安全冷启动。

---

## 16. 现状缺口与处置结论

> 以下为从代码现状中发现的**行为缺口或未定义点**，已逐项定案并落地。**新增/修改的行为以本文档其他章节为准**（本节仅记录决策与口径）。

| # | 缺口 | 处置 | 落地口径 |
|---|---|---|---|
| D1 | **无平局判定** | ✅ 已实现 | 满盘（第 225 手落定）未成五连即和棋：引擎发 `Draw` 事件、`over=true`、`winner=null`；**五连优先**（最后一手同时成五按胜局）；和棋不加胜场、停表、横幅 `双方和棋`；悔棋可解除和棋。三端 UI 与联机（两端各自本地推导，无新协议字节）均支持。**联机要求两端同版本**：旧版无和棋逻辑，第 225 手后会继续落子导致永久失步（与 TIMEOUT 同性质） |
| D2 | **聊天只收不发** | ✅ 已实现 | 对话弹窗补输入框 + 发送按钮（软键盘动作同为发送）；回复对象 = 最近一条来信的对端 IP；本地即时回显（署名 `自己(本机IP)`）；>255 字节按 UTF-8 边界自动截断。协议无改动（复用 CHAT=14 既有字节） |
| D3 | **五连终局后联机仍可发起悔棋** | ✅ 已实现 | 终局后本方禁发悔棋/求和；收到协商请求一律回执 REJECT（悔棋回 ROLLBACK_REJECT、求和回 DRAW_REJECT），不再静默丢包；已开弹窗作废关闭；点"同意"也不回包不回退。悔棋/重开合法解除终局后标志复位、可继续落子 |
| D4 | **联机无假死检测** | ✅ 已实现 | 追加 `HEARTBEAT=11`（空 payload，仅追加不改既有字节）：连接成功后每 5s 发送；**收到过对端心跳后**距最后收帧 ≥15s 判死（检测窗口 ≤20s），走既有断线路径（停表 → `对方已断开连接` → 退出）。旧版对端从不发心跳 → 本端只发不判，**不误报**。局域网/蓝牙共用同一实现 |
| D5 | **悔棋不回退比分** | ✅ 已文档化 | **明确口径：比分不回收**。终局后悔棋解除终局，但已计入的胜场保留（本地双人/人机/联机三端一致）。见 FR-NET-11 |
| D6 | **蓝牙对局页标题仍为「联网对战」** | ✅ 已实现 | 对局页按 `bluetooth` 参数显示 `联网对战` / `蓝牙对战`（§2.1） |
| D7 | **部分文案硬编码** | ✅ 已实现 | 全部提示/Toast 收敛到 `strings.xml`，Effect 只携带 `@StringRes`；文案值不变（§12.6）。取舍：release 字符串加密不覆盖资源文件，移入 res 后这些文案不再被加密（非敏感信息，可接受） |
| D8 | **设备离线残留** | ✅ 已实现 | 联机页每 10s 重播 JOIN（在线设备借此刷新）；30s 未刷新自动淘汰（对端崩溃也能清退）；同名重复刷新原地更新不抖动，改名才后移。蓝牙页保持现状（列表以已配对设备为主体，静态不淘汰） |
| D9 | **互戳裁定用字符串比较 IP** | ✅ 仅文档化 | **保持字符串字典序**，不改。理由：双端必须同规则，改成数值比较会让新旧版本混用时双方都自认 server（双黑）；字典序的"不符合直觉"仅影响谁当 server（不影响公平性与胜率）。跨端约定同一规则即可（§9.2 FR-LAN-13、§13 红线 8） |
| D10 | **iOS 蓝牙互通不可行** | ✅ 仅文档化 | 平台限制（iOS 无公开 RFCOMM/SPP），**不做代码改动**。iOS 蓝牙对局只能同平台（MFi/BLE 方案），或不做；局域网互通不受影响（§13） |
